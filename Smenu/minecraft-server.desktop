[Desktop Entry]
Name=Gestionar Servidor Minecraft
Comment=Panel de administración del servidor Bedrock
Exec=bash -c 'BASE=$(find "$HOME" "/mnt/Windows" -type d -name "Smenu" 2>/dev/null | head -n 1); if [ -n "$BASE" ] && [ -f "$BASE/bedrock-server-automenu.sh" ]; then cd "$BASE"; ./bedrock-server-automenu.sh; else echo "❌ No se encontró el script en ninguna carpeta Smenu"; read; fi'
Icon=utilities-terminal
Terminal=true
Type=Application
Categories=Utility;Game;
